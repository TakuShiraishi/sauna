<% if user_signed_in? %>
    <%= form_with url: search_result_searchs_path, method: :get do |f| %>
      <%= f.collection_select :prefecture_eq, JpPrefecture::Prefecture.all, :name, :name, { include_blank: '都道府県' }, { class: 'custom-select' } %>
      <%= f.submit '検索' %>
    <% end %>
<% end %>