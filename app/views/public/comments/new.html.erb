<%= render 'layouts/header'%>
<div class="container">
　<div class="row">
　  <div class="col-md-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
     <h2>コメントを書き込む</h2>
      <%= form_with model: [@sauna, @comment], local: true do |f| %>
       <%= f.text_area :comment, rows: '5',cols: '55', placeholder: "コメントを入力" %>
       <%= f.submit "送信する" %>
       <% end %>
    </div>
  </div>
</div>
<%= render 'layouts/footer'%>